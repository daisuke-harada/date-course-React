export type PrefectureData = {
    id: number,
    name: string,
    areaId: number
};

export type AreaData = {
    id: number,
    name: string
};

export type GenreData = {
    id: number,
    name: string
};